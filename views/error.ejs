<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>JJ Fashion - Error</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <!-- Bootstrap CSS CDN -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>

    .error-container {
      margin-top: 80px;
      text-align: center;
    }

    .error-code {
      font-size: 100px;
      font-weight: 700;
      color: #e74c3c;
    }

    .message {
      font-size: 24px;
      color: #555;
    }

    .countdown {
      margin-top: 15px;
      color: #888;
    }
  </style>

  <script>
    let seconds = 10;
    setInterval(() => {
      seconds--;
      document.getElementById("timer").innerText = seconds;
      if(seconds === 0) {
        window.location.href = "/";
      }
    }, 1000);
  </script>
</head>
<body>

  <div class="container error-container">
    <div class="error-code"><%= statusCode %></div>
    <div class="message"><%= message %></div>
    <div class="countdown">
      Redirecting to <a href="/">Home</a> in <span id="timer">7</span> seconds...
    </div>
    <a href="/" class="btn btn-danger mt-4">Go Home Now</a>
  </div>

</body>
</html>
